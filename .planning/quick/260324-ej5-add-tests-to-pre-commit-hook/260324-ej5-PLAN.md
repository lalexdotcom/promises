# Quick Task 260324-ej5: Add Tests to Pre-commit Hook — PLAN

**Created:** 2026-03-24  
**Status:** Ready for execution

## Goal

Étendre le hook `pre-commit` pour qu'il exécute aussi la suite de tests (`pnpm test`) après le lint/format, bloquant le commit si un test échoue.

## Current State

```json
"simple-git-hooks": {
  "pre-commit": "pnpm exec lint-staged"
},
"lint-staged": {
  "*.{ts,js}": "biome check --write"
}
```

Hook installed at `.git/hooks/pre-commit` → runs only `pnpm exec lint-staged`.

## Plan

### Task 1 — Modify package.json pre-commit command

**File:** `package.json`  
**Change:** update `simple-git-hooks.pre-commit` to chain tests after lint-staged

```json
"pre-commit": "pnpm exec lint-staged && pnpm test"
```

### Task 2 — Reinstall the git hook

```bash
pnpm simple-git-hooks
```

This regenerates `.git/hooks/pre-commit` with the new command.

### Task 3 — Verify

- `cat .git/hooks/pre-commit` → must contain `pnpm test`
- `pnpm run test` → all 156 tests pass

### Task 4 — Commit

```bash
git add package.json
git commit -m "feat(quick-260324-ej5): add tests to pre-commit hook"
```

## Success Criteria

- [ ] Pre-commit hook runs `pnpm exec lint-staged && pnpm test`
- [ ] `.git/hooks/pre-commit` reflects the new command
- [ ] All tests still pass (156)
